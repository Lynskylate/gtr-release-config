groups:
  gtr-core:
    ssh_host: gtr.tail414c32.ts.net
    ssh_user: ci
    ssh_port: 22
    require_sudo: true
    service_root: /srv/projects
    secret_root: /srv/project-secrets
    default_healthcheck_timeout_seconds: 60
  edge-aliyun:
    ssh_host: 100.102.140.59
    ssh_user: ci
    ssh_port: 22
    require_sudo: true
    service_root: /srv/projects
    secret_root: /srv/project-secrets
    default_healthcheck_timeout_seconds: 60
  edge-tencent:
    ssh_host: 100.99.48.76
    ssh_user: ci
    ssh_port: 22
    require_sudo: true
    service_root: /srv/projects
    secret_root: /srv/project-secrets
    default_healthcheck_timeout_seconds: 60

